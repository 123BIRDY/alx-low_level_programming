#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @ab: input integer
 * Return: Always success
 */

int _abs(int ab)
{
return (ab * ((ab > 0) - (ab < 0)));
}
