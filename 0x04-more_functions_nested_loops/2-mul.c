#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: first set of integers to multiply
 * @b: second set of integers to be multiplied
 *
 * Return: 0 when successful
 */

int mul(int a, int b)
{
	int answer = a * b;

	return (answer);
}
