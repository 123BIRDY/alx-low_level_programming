#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @t1: first integer to be calculated
 * @t2: second integer to be calculated
 * Return: the result
 */

int add(int t1, int t2)
{
	return (t1 + t2);
}
